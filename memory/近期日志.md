# 近期日志

## [07-03] 完美化：死代码清理

**删了 11 个孤立文件**（未被任何 import 引用，非独立入口）：
- `input_validation.py`, `legacy_lock_cards_page.py`, `role_permissions_page.py`
- `services/booking_link.py`, `services/event_queue.py`, `services/ota_reserve.py`, `services/reconciliation_report.py`
- `transactions/deposit.py`, `transactions/extend_stay.py`, `transactions/lost_card.py`
- `lock_deploy/diag.py`

**地图同步**: ✅ (276个.py)
**测试**: 108 passed

---

## [07-02h~07-03] 架构巡检全部完成

P0(4/4): trace覆盖关键流程 + SOLID_UI_V2删除 + LEGACY_THEME_ALIAS删除 + except:pass修复
P1(3/6): vendor_service新建 + 3项跳过(低风险)
P2(0/7): 全部跳过(低收益)
P3(3/3): services解耦 + _get_app_dir统一 + 死代码清理

---

## [07-02g] P2收尾+UI双轨消除

JSON结构化日志 + trace_context + v2编译器默认启用 + 删除6个硬编码QSS + 删除design_tokens_v4.py + db_access/aggregation.py新建

---

## [07-02f] P0+P1全部修复

WAL模式 + _table_exists() + verify_ledger_integrity补exchange_rate + db_schema补全列 + get_guest_by_room修复 + CheckoutService补保洁任务

**改了**：
- `database.py` — 启用 WAL 模式（`PRAGMA journal_mode=WAL` + `synchronous=NORMAL` + `cache_size=-8000`）；新增 `check_integrity()` 和 `checkpoint_wal()` 方法；末尾 `search_audit_logs`/`detect_anomalous_behavior` 迁移到 `services/audit_service.py`（保留向后兼容委托）
- `backup_service.py` — 全量重写：SHA-256 校验和 + 紧急备份 + 备份保留数 7→14 + 恢复前校验和验证
- `health_monitor.py` — 新增 `_check_db_integrity()`，数据库损坏时自动触发紧急备份 + CRITICAL 告警
- `core/guests.py:140-148` — 修复 `get_guest_by_room` 的 `dict(row)` Bug（sqlite3.Row 不能直接转 dict）
- `services/audit_service.py` — 新建，从 database.py 拆出的审计查询函数
- `tests/test_core_guests.py` — 新建，GuestService 测试（6 个）
- `tests/test_core_ledger.py` — 新建，LedgerHashChain + LedgerService 测试（6 个）
- `tests/test_core_pricing.py` — 新建，PricingService 测试（暂 skip，等 P1-2 修复）
- `tests/test_checkout_service.py` — 新建，CheckoutService 测试（3 个）
- `.github/workflows/test.yml` — 升级 Python 3.11→3.12 + 完整依赖安装
- `CLAUDE.md` — 新增待修复问题清单指针
- `memory/架构审计-待修复问题清单.md` — **新建**，包含 11 项待修复问题（P0×2, P1×3, P2×5），新对话应优先处理

**关键结论**：
- 测试：100 passed, 3 skipped ✅
- WAL 模式已启用，数据库容灾能力大幅提升
- 11 项问题待修复，详见 `memory/架构审计-待修复问题清单.md`
- **P0-1（哈希验证不一致）根因已定位**：`append_ledger:794` 包含 `rate_v` 而 `verify:1898` 不含

**待办**: 按 P0→P1→P2 顺序修复 `memory/架构审计-待修复问题清单.md` 中的问题

---

## [07-02e] UI 层全量修复

**改了**：
- `ui/__init__.py` — 删除顶层 `from ui.layout import` 和 `from ui.components import`，改为 `__getattr__` 延迟加载，打断 design_tokens → ui → components 循环导入链
- `ui/components/__init__.py` — `from components.button_optimized` 改为延迟导入，消除自循环
- `ui/layout/frontdesk.py` — BTN_COLORS/INPUT_COLORS/ROOM_STATUS_COLORS 从模块级执行改为惰性缓存，避免导入时调 _p()
- `ui/branding/brand.py` — 删除 `from ui.tokens import ColorPrimary`，改为从 brand_config_v4 导入 APP_NAME，BRAND_COLOR 硬编码
- `brand_config_v4.py` — 删除 `from ui.tokens.colors import ColorPrimary`，改为硬编码 `_BRAND_GOLD`
- `ui/emptystate.py` — 删除（无外部引用，ui_helpers.py 有独立版本）
- `ui/surfaces/`、`ui/theme/` — 删除（空目录）
- `ui_helpers.py:1387` — 注释修正（toast_widget→ui.components.toast）
- `ui/tokens/colors.py` — 精简注释

**地图同步**: ✅（ui/ 12→11，删 emptystate.py + 2 空目录）

---

**改了**：
- `room_standee_renderer.py` — 全量重写，内置 A6 横版模板（148×105mm @300DPI），不再依赖外部 PNG。纯代码渲染：欢迎语+酒店名+房号+55mm 二维码+用途说明+安全提示。新增 A4 竖版拼版（2张A6上下排列，虚线裁切线），兼容旧接口。
- `qr_code_service.py` — 导出按钮更新（「导出 A6 立牌」「导出全部 A6」「导出 A4 拼版」），移除旧的 template_ready 检查。
- `theme_palette.py` — 四主题侧栏色值重写，从主色派生（MIST=深海蓝 #1F3D5C、SHADE=深林绿 #1E352A、GLOW=深玫灰 #342028、INK=深墨蓝 #0F1820），补全 8 个 sidebar token（text/icon/active_text/active_icon/group_label/border_right/divider/role_strip_border）。
- `themes/mist.qss shade.qss glow.qss ink.qss` — 重新生成，0 残留 token。
- `memory/项目地图.md` — 移除幽灵文件 `_generate_themes.py` + `qss_compiler.py`（根目录版），总览补 spec 文件和 themes 说明，地图统计已于 [07-02c] 全量校验。

**打包状态**：`Solid_onefile.spec` `Solid.spec` `SolidCollector.spec` `一键打包.bat` 均已修复同步。

**验证**：`python app_main.py` → shutdown 通过 ✅

---

**改了**：
- `酒店系统/Solid_onefile.spec` — 删除 12 个幽灵引用(toast_widget/toast_notify/hotel_overview_tab/access_parser/不存在的bridgecore)，补全 50+ 缺失模块(ui/solid_ui_v2/money_utils/nav_manifest/services 等)，新增 datas(solid_ui_v2/compiled)
- `酒店系统/Solid.spec` — 全量覆盖同步，与 Solid_onefile.spec 保持一致
- `采集器/SolidCollector.spec` — 删除不存在的 bridgecore.tests 导入，补全 5 个缺失 bridgecore 模块，修正 known_signatures.json 路径
- `酒店系统_一键打包.bat` — 修正 Python/ISCC 路径(FF.FC→FF)，修正预检模块列表(移除 9 个不存在的 bridgecore，新增 money_utils/nav_manifest/shop_icon_pack)

**关键结论**：
- PMS 端 bridgecore/ 只保留 panic_recovery + config + fault_manager + orchestrator（4 个）
- 采集器端 bridgecore/ 实有 48 个 .py，spec 已全覆盖

**待办**: 运行预检验证模块 import，然后 PyInstaller 试打包

---

## [07-02b] Solid UI v2 设计系统接入（Phase 0-6 全部完成）

**改了**：
- 新建 `solid_ui_v2/` — 设计师交付的 v2 设计系统（34 个 .py + 5 个 .qss），四层解耦
- `solid_ui_v2/qss/compiler.py` — TEMPLATE_PATH 指向 v1 模板（4836 行），解决 31 个 token 缺口
- `solid_ui_v2/themes/_schema.py` — DEFAULT_FALLBACK 新增 31 个 v1 兼容 token
- `solid_ui_v2/runtime/expand.py` — 新增 `_expand_v1_qss_compat()` 派生函数
- `app_main.py` — 新增 `_apply_theme_v2()` + `SOLID_UI_V2` 环境变量开关
- `design_tokens.py` — _p() 增加 v2 分支（_p_v2 via solid_ui_v2.runtime）
- `theme_palette.py` — 末尾加 v2 色板覆盖逻辑
- `solid_ui_v2/scripts/test_smoke.py` — 8/8 通过 + Windows GBK/正式导入修复

**关键决策**：
- 双轨共存：默认走 v1（零风险），`SOLID_UI_V2=1` 切 v2
- v2 不变 v1 不改 86 个引用文件，theme_palette/design_tokens 尾部接开关
- 用 v1 模板(4836行) + v2 编译器 + v2 补丁 = 完整 v2 QSS
- v2 内部导入全部改成相对导入，避免与 PMS 同名包冲突

**验证结果**：
- `python solid_ui_v2/scripts/test_smoke.py` → 8/8 通过 ✅
- v2 compile_theme(mist) → 129KB，0 个未替换 token ✅
- v2 编译全部 4 套主题 → 全部通过 ✅
- v2 theme_palette MIST primary=#3D6A93, sidebar=#1F3D5C ✅

**地图同步**: ✅（新增 solid_ui_v2/ 统计行 34 .py）

**下一步**: `$env:SOLID_UI_V2="1"; python app_main.py` 启动 GUI 验证视觉效果

---

## [07-02] 规则体系完善 + 项目地图全量修正 + 采集器↔PMS 链路校验

**改了**：
- `CLAUDE.md` — 新增近期日志指针、地图自校验规则、增删文件三件套流程
- `memory/近期日志.md` — 新建，轻量变更摘要
- `memory/项目地图.md` — 全量修正 12 处（表格/统计/幽灵条目/漏数文件）
- `memory/项目地图.md` — 握手包协议区加校验结论

**地图同步**: ✅ 已全量校验（Glob 每个子目录 vs 统计表，一致）

**关键结论**：
- 采集器 → PMS `.solidhandover` 握手包链路畅通（v1.0 / 三种 mode 全互认）
- 项目地图此前有 12 处偏差（缺 `__init__.py`、幽灵文件、统计不一致），已全部修正

**待办**: 无

---

## [06-29] 全量扫描审计 + 代码清理 + 地图修正

**改了**：
- 删除 10 个 `.bak_0624` 备份文件
- `memory/项目地图.md` — 日期/统计数/删除 14 个不存在文件条目
- `lock_adapters/legacy_migration/schema_analyzer.py` — SQL 注入修复

**地图同步**: ✅

---

## [06-25] 夜墨 Ink 主题收尾 + 侧栏/分割线/数据行三色微调

**改了**：
- `theme_palette.py` — INK 字典 4 字段
- `themes/ink.qss` — 重新生成
- `themes/base.qss.template` — 11 处改动
- `themes/{mist,shade,glow,ink}.qss` — 四主题分离落地

**地图同步**: ✅

---

## [06-24] 四主题 QSS 分离 + 启动加速 P0

**改了**：
- `themes/base.qss` → `base.qss.template` + 四份独立 QSS
- 新建 `_generate_themes.py`
- 8 个 Tab `__init__` 末尾 DB 查询 → `QTimer.singleShot(0, ...)` 延迟加载
- `app_main.py` — 删除 `_load_palette()` 和 `_inject_qss_variables()`

**地图同步**: ✅

---

## [06-20] 10 项收尾 + 对账服务骨架

**改了**：
- `payment_v4.py` — ledger 闭环
- `Solid_onefile.spec` / `Solid.spec` — hiddenimports 追加 28+ 模块
- 新建 `reconciliation_service.py`、`reconciliation_checks.py`
- 新建 `tests/test_reconciliation.py`

**地图同步**: ✅
