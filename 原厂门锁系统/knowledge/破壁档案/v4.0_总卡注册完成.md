# 门锁破拆 v4.0 — 总卡注册完成

- 时间：2026-05-31 16:30~17:00 (UTC+7)
- 前情：`v3.0_F2补丁通过_等总卡.md`（F2 工程模式补丁已通，等总卡）
- 状态：**注册成功，酒店系统已重新激活**

---

## 一句话总结

**总卡（配对套件，两张同步，指纹 `C92B20B7FF00...`）于 2026-05-31 16:31 完成系统重装注册**。`System.ini` 的 `PCID/SN/LD` 已刷新，`HotelName=888` 写入。发卡器原版 `CardLock.exe` 可以不依赖 `CardLock_F2.exe` 补丁正常启动。

---

## 本次动作时间线

| 时间 | 动作 |
|---|---|
| 16:15 | 用户确认拿到总卡（两张，之前从酒店带回） |
| 16:24-16:27 | 注册前基线备份：`System.ini`、进程快照、F2 补丁校验、注册表关键词扫描 |
| 16:29 | `_watch_eng_flag.py` 确认补丁版 flag=1 |
| 16:31 | 注册成功 |
| 16:32 | 注册后基线抓取 |

---

## 注册后 System.ini 变更

| 字段 | 注册前 | 注册后 |
|---|---|---|
| `PCID` | `F0421C762695` | `F020FF16D2BC` |
| `SN` | `C0235BDA…A4B7537F`（短） | `31C6B53B…ECEFF88CF3D6`（更长） |
| `LD` | 过期态 | `E5BC2456664279810E4454037D207DDC` |
| `HotelName` | （空白） | `888` |
| `HotelID` | `003D34CF0528378A0189859F02462FD0` | 不变 |
| `dlsCoID` | `2826423`（韩亚斌经销商） | 不变 |

→ 系统重新签发了本机凭据。`HotelID`（酒店身份证）和 `dlsCoID`（经销商）保持不变。

---

## 总卡配对套件（重要存档）

### 来源
- 从酒店带回来的两张总卡（卡套印字标识确认）
- **不是**经销商新寄的总卡
- 是酒店现役能开门的两张总卡

### MDB 历史指纹对照（`cards_corpus_v2_dbbak02.md` #13/#14）

| 序号 | 卡数据（16 字节 hex） | 出现次数 | 首见 MDB |
|---|---|---|---|
| #13 | `C92B20B7FF006C6100BC152C0A252DFB` | 6 | CardLock260517.MDB |
| #14 | `C92B20B7FF00229800BB155D13255DFB` | 6 | CardLock260517.MDB |

**特征**：
- 前 8 字节 `C92B20B7FF00`（旧总卡是 `C92B20B7AA00`，`FF00` 标全新批次）
- 两张同一天（2026-05-17）首次被 MDB 记录、各出现 6 次
- **不是空白未激活卡**，而是已经在中心库注册、等本地激活的半成品

### 处置建议
- 选一张物理品相好的当主卡（`Master_A`），另一张 `Master_B` 封袋锁保险柜
- **永远不要把两张卡同时放发卡器上**
- 万一日后需要：备份卡应可直接使用（指纹模式相同）

---

## 用户后续操作（未提交到磁盘）

用户做完注册后继续测试层控卡：
- 读层控卡 → 注销 → 再读
- 由于 `CardLock.exe` 仍在运行，Access Jet 引擎将改动缓存在内存
- 当前 `CardLock.mdb` 磁盘状态仍为 06:49:30
- 如需看新数据，需关闭 `CardLock.exe` 让 Jet flush

---

## MDB 快照（注册后基线）

| 表 | 行数 | 说明 |
|---|---|---:|---|
| CardInfo | 216（全是客人卡） | 无总卡/层控卡记录 |
| CKCard | 41 | 校验哈希 |
| GuestInfo | 213 | 客人住宿 |
| OperatorInfo | 6 | Admin / Super / proQuanXian_* |
| BuildingInfo | 1 | 楼栋号 1 |
| Dai | 16 行全 0 | 卡类型映射（注册后未使用） |
| RoomInfo | 26 | 房号 |
| RegInfo | **0** | 注册信息表空（注册记录只写 `System.ini`） |
| CaoZuoInfo | **0** | 操作日志空 |
| LogInfo | **0** | 登录日志空 |
| RecordOpen | **0** | 开锁记录空 |

---

## 当前状态（会话结束时）

- `CardLock.exe` 原版运行中（不需要 F2 补丁了）
- 持有 `CardLock.mdb` 会话锁（`CardLock.ldb` 16:35:52）
- 系统已注册，理论上回酒店可正常开门
- 发卡器 LED 状态请用户现场确认

---

## 已备份基线

| 基线 | 路径 |
|---|---|
| 注册前 | `reports/pre_register_20260531_162752/` |
| 注册后 | `reports/post_register_20260531_163240/` |
| F2 补丁版 exe | `智能门锁管理系统新2021网络版\CardLock_F2.exe`（md5 `d66ac5ce0e52ccf0effe811a152d7d86`） |

### 预注册基线内容
- `System.ini.pre_register.bak`（1725B）
- `check_f2_patch.txt`（3 个 exe 补丁校验）
- `check_running_patch.txt`（内存中确认 3B C0）
- `process_snapshot.txt`（`CardLock_F2.exe` 运行中）
- `scan_reg_strings_fixed.txt`（关键词扫描）
- `scan_reg_strings.txt`（原版扫描输出）

### 后注册基线内容
- `System.ini.post_register.bak`（1725B，新 PCID/SN/LD）
- `check_running_patch.txt`（补丁已从内存退出）
- `CardLock.mdb.post_register.bak`（970752B，06:49:30 状态）

---

## 回酒店前要做的事

1. **关闭 CardLock.exe** → 让 MDB Jet 缓存 flush 到磁盘
2. **重新启动 CardLock.exe** → 确认标题栏显示酒店名 `888`
3. **跑 `_scan_reg_strings.ps1` 做注册后扫描** → 与注册前基线 diff
4. **把 `Master_B` 卡封袋放保险柜**
5. **打包 `reports/` 下所有基线到 U 盘** → 带回去

---

## 给下一个 AI 的提示词模板

> 已读 `卡_lock壁_history/` 时间线 v1.0~v4.0。门锁破解完成状态：总卡注册成功（`System.ini` PCID/SN/LD 已刷新，HotelName=888）。`CardLock.exe` 原版已可正常启动。两张配对总卡（`C92B20B7FF00...`，对应 MDB #13/#14，2026-05-17 首次出现）分别在用和封存。用户测试过层控卡操作但仍未关闭 CardLock → MDB 未 flush。完整背景见 `reports/卡_lock壁_history/v4.0_注册完成.md`。

---

编写人：AI Agent（本文档基于 2026-05-31 16:00-17:00 对话生成）
