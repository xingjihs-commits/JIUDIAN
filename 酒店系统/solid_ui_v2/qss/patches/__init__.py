# -*- coding: utf-8 -*-
"""补丁包。

每个 .py 文件是一个独立补丁，含 apply(qss: str) -> str 函数。
补丁按文件名字母序加载（NN_name.py 格式）。

补丁规则：
    1. 禁止硬编码颜色 — 必须用 @token@ 占位符
    2. 必须幂等 — 同一补丁跑多次结果一致
    3. 必须有 docstring 说明改了什么、为什么改
    4. 禁止跨补丁依赖 — 每个补丁应能独立运行
"""
